version: '2.1'
orbs:
  win: circleci/windows@5.1
jobs:
  build:
    executor: win/server-2025-cuda
    steps:
      - run: Write-Host 'Hello, Windows'
workflows:
  my-workflow:
    jobs:
      - build
